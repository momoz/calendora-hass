<!-- Vendored on 2026-08-06 from Calendora's design source, rendered from the icon the
     app itself ships. VENDORED: do not edit these files. Changes are re-rendered from
     source by the maintainer, who tracks the exact revision on the private side. -->

# Brand assets

`icon.png` (256×256) and `icon@2x.png` (512×512) are Calendora's real mark: a
teal-blue (`#1e6a8c`) rounded square with a white arc and a dot. They are
pixel-identical to what the app ships.

Home Assistant 2026.3 moved brand assets into the integration itself; PRs to the
`home-assistant/brands` repo for custom integrations are auto-closed, so this
directory is the only channel there is.

**These are vendored, exactly like `docs/API-SURFACE.md`.** Do not redraw them,
do not regenerate them, do not adjust them to taste. A change starts in the
design source, is re-rendered, and is re-vendored here with this header updated.
